# Packages

Shared implementation-safe packages live here.

Current packages:
- `domain`: canonical enums, IDs, and entities
- `integration-contracts`: adapter interfaces, shared signal/result types, and in-memory mocks
