# FLAMINGO-P1-010 Assumptions and Unknowns Policy — Open Items

**Last Updated:** 2026-04-11
**All items are non-blocking for v1 foundation.**

---

## Internal Decisions Required

- [ ] **UI-P10-001** — Confirm whether additional resolution-owner tag types are needed beyond the current four (`[REQUIRES REG D COUNSEL INPUT]`, `[REQUIRES SECURITIZE CONFIRMATION]`, `[REQUIRES SECOND STREET INPUT]`, `[REQUIRES INTERNAL DECISION]`). Specifically: if a third-party compliance vendor, blockchain infrastructure vendor, or external auditor joins the v1 ecosystem, a named tag for that party would need to be added to the tag vocabulary and to this policy document. The current tag set is sufficient for all parties currently named in the v1 scope.
  `[REQUIRES INTERNAL DECISION]`

- [ ] **UI-P10-002** — Confirm whether the 8-status document status model defined in Section 11 requires additional states for operational workflow tracking. Specifically: whether `IN LEGAL REVIEW` and `IN VENDOR REVIEW` should exist as distinct statuses from `WAITING ON LEGAL` and `WAITING ON VENDOR`, to distinguish between "actively in review by the party" and "waiting for the party to begin review." The current model treats these as the same status. If document tracking requires finer granularity, this section must be updated.
  `[REQUIRES INTERNAL DECISION]`
