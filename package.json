{
  "name": "title2token-phase3",
  "private": true,
  "version": "0.1.0",
  "description": "Phase 3 implementation-prep workspace for Title2Token / Flamingo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
